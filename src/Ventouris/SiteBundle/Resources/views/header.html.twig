<div>
    <nav class="top-bar">
        <ul>
            <!-- Title Area -->
            <li class="name">
                <h1>
                    <a href="{{path('adminredir')}}">
                        Ventouris
                    </a>
                </h1>
            </li>
            <li class="toggle-topbar"><a href="#"></a></li>
        </ul>


        <section>
            <!-- Left Nav Section -->
            <ul class="left">
                {% if is_granted('ROLE_ADMIN') %}
                <li><a href="{{path('adminredir')}}">Admin</a></li>
                {% endif %}
                <li class="divider hide-for-small"></li>
            </ul>

            <!-- Right Nav Section -->
            <ul class="right">
                <li class="divider show-for-medium-and-up"></li>
                {% if is_granted('ROLE_USER') %}
                <li class="has-dropdown">
                    <a href="#">{{app.user.username}}</a>
                    <ul class="dropdown"><li class="title back js-generated"><h5><a href="#">Name</a></h5></li>
                            <li><a href="{{path('fos_user_profile_edit')}}">Προφίλ</a></li>
                            <li class="divider"></li>
                            <li><a href="{{path('fos_user_security_logout')}}">Αποσύνδεση</a></li>
                    </ul>
                </li>
                {% else %}
                <li><a href="{{path('fos_user_security_login')}}">Σύνδεση</a></li>
                <li><a href="{{path('fos_user_registration_register')}}">Εγγραφή</a></li>
                {% endif %}
            </ul>
        </section>
    </nav>
</div>